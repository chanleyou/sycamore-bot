import moment from 'moment'

export const MESSAGE_DELETE_TIMEOUT = moment.duration(36, 'hours')
export const CLEAR_INTERVAL = moment.duration(1, 'hour')
// export const MESSAGE_DELETE_TIMEOUT = moment.duration(5, 'seconds')
// export const CLEAR_INTERVAL = moment.duration(1, 'second')
